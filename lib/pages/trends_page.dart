import 'package:flutter/material.dart';

class TrendsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Trends Page (Empty for now)'));
  }
}